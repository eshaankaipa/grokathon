# Copy to .env (gitignored) and fill in real values. NEVER commit real secrets.
# X API — app-only bearer is used for all ingestion (read-only).
X_BEARER_TOKEN=
# X API — OAuth 1.0a (reserved for publishing; not used by ingestion).
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=
# xAI Grok — semantic classifier.
XAI_API_KEY=
